# Transcription worker dependencies (Apple Silicon / macOS only).
#
# mlx-whisper is built on Apple's MLX framework and only runs on Apple Silicon,
# so the worker must run natively on the host — it cannot run in the Linux API
# Docker image. Install with:  pip install -r requirements-worker.txt
#
# Includes the base API deps because the worker imports app.database / app.services.
-r requirements.txt

mlx-whisper>=0.4.0
numpy>=1.24
