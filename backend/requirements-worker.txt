# Transcription worker dependencies (Linux x86_64, CPU or CUDA).
#
# faster-whisper uses CTranslate2 and runs on CPU (int8) or NVIDIA GPU (float16).
# Install with:  pip install -r requirements-worker.txt
#
# Includes the base API deps because the worker imports app.database / app.services.
-r requirements.txt

faster-whisper>=1.0.0
